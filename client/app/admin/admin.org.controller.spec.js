/*
'use strict';

describe('Controller: AdminOrgCreateCtrl', function () {

  // load the controller's module
  beforeEach(module('tviundApp'));

  var AdminOrgCreateCtrl, scope;

  // Initialize the controller and a mock scope
  beforeEach(inject(function ($controller, $rootScope) {
    scope = $rootScope.$new();
    AdminOrgCreateCtrl = $controller('AdminOrgCreateCtrl', {
      $scope: scope
    });
  }));

  it('should ...', function () {
    expect(1).toEqual(1);
  });
});
*/
